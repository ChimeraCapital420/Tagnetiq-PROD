# Tagnetiq Environment Variables - Secure Naming Convention
# Copy this file to .env.local and fill in your actual tokens/secrets

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_TOKEN=your_supabase_anon_token
SUPABASE_SERVICE_SECRET=your_supabase_service_role_secret

# AI API Tokens - The Hydra Multi-AI System
# Claude (Anthropic)
ANTHROPIC_SECRET=your_anthropic_secret

# OpenAI (GPT-4 Vision & GPT-4)
OPENAI_TOKEN=your_openai_token

# Google AI (Gemini)
GOOGLE_AI_TOKEN=your_google_ai_token

# xAI (Grok)
XAI_SECRET=your_xai_secret

# DeepSeek
DEEPSEEK_TOKEN=your_deepseek_token

# Image Processing
# remove.bg API for Clean Room feature
REMOVE_BG_SECRET=your_remove_bg_secret

# Product Database APIs (for barcode lookup)
UPC_DATABASE_TOKEN=your_upc_database_token
BARCODE_SPIDER_TOKEN=your_barcode_spider_token

# Optional: External Market Data APIs
AMAZON_API_TOKEN=your_amazon_product_api_token
EBAY_API_TOKEN=your_ebay_api_token

# Security
JWT_SECRET=your_jwt_secret_string
ENCRYPTION_SECRET=your_encryption_secret_for_user_data

# Vercel Configuration (automatically set by Vercel)
VERCEL_URL=your_vercel_deployment_url

# Development
NODE_ENV=development

# API Rate Limiting
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# Subscription Tiers Configuration
FREE_TIER_SCANS_PER_MONTH=50
PRO_TIER_SCANS_PER_MONTH=1000
ENTERPRISE_TIER_SCANS_PER_MONTH=10000

# AI Model Preferences (fallback order)
PRIMARY_AI_MODEL=claude
SECONDARY_AI_MODEL=gpt4
TERTIARY_AI_MODEL=gemini

# Feature Flags
ENABLE_VOICE_RECOGNITION=true
ENABLE_CLEAN_ROOM=true
ENABLE_AUTO_LISTING=true
ENABLE_PROFIT_TRACKING=true

# Webhook URLs (for external integrations)
DISCORD_WEBHOOK=your_discord_webhook_for_notifications
SLACK_WEBHOOK=your_slack_webhook_for_alerts

# Analytics & Monitoring
SENTRY_DSN=your_sentry_dsn_for_error_tracking
GOOGLE_ANALYTICS_ID=your_google_analytics_id

# Email Configuration (for notifications)
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
FROM_EMAIL=noreply@tagnetiq.com

# Cloud Storage (for user uploads)
AWS_ACCESS_ID=your_aws_access_id
AWS_SECRET=your_aws_secret
AWS_S3_BUCKET=tagnetiq-user-uploads
AWS_REGION=us-east-1

# Database Backup (optional)
DATABASE_BACKUP_URL=your_database_backup_service_url