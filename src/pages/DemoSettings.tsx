// src/pages/DemoSettings.tsx
import React from 'react';
import Settings from './Settings'; // Re-using the main settings for the demo

const DemoSettings: React.FC = () => {
  return <Settings />;
};

export default DemoSettings;